#include <stdio.h>
#include <iostream>
#include <time.h>
#include <string>
using namespace std;

/// Вариант 10 (Высокий уровень) - Написать функцию, создающую файл с данными о дисплеях из не менее 8 записей, осуществляющего его сортировку по заданому параметру.///

struct display
{
    char mode[50];
    int price;
    double x_size;
    double y_size;
    int optr;
};
 
void task10()
{
int N = 8;
    display Ms[N];
    
bool greater (int a, int b)
{
return (a>b);
}

sort(Ms.begin(), Ms.end(), greater);
 
   cout << "\nBook mode: " << Ms[i].name;
cout << "\nBook price: " << Ms[i].authors;
cout << "\nBook pages " << Ms[i].pages;
cout << "\nBook x_size: " << Ms[i].circulation;
cout << "\nBook y_size: " << Ms[i].published; 
}
}
int main()
{
    void task10();
}
